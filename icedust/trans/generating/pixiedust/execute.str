module generating/pixiedust/execute

imports
  signatures/pixiedust/Types-sig
  
  generating/pixiedust/expression
  
  api/types-names-api

rules

/* replaced by cast
  execute-expression-serialize(|i): e -> result
    where 
      View() := <get-type2> e
    with
      (exp_e, stmts_e, i') := <exp-to-js-stmts(|i)> e
    ; exp_result := js-exp|[ReactDOM.renderToStaticMarkup(exp_e)]|
    ; result := (exp_result, stmts_e, i')
*/